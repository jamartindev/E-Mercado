document.addEventListener("DOMContentLoaded", async () => {
const productID = localStorage.getItem("id");

console.log(productID);

})


//Aca me esta tomando el id del localStorage
// Ahora queda armar la funcion para que muestre los datos del producto
// La funcion para que me guarde el id esta en el product al final de todo
